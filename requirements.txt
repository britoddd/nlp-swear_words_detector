# Runtime dependencies for the Streamlit web app (Hugging Face Space).
# Pinned for reproducibility; scikit-learn MUST match the version that produced
# the .pkl models (1.7.1) or unpickling warns / breaks.

streamlit==1.39.0
torch==2.2.2
transformers==4.42.3
tokenizers==0.19.1
safetensors>=0.4.3
huggingface_hub>=0.23.4
scikit-learn==1.7.1
numpy==1.26.4
pandas==2.1.4
nltk>=3.8.1
PySastrawi==1.2.0
tqdm>=4.66
